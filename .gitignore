functions/node_modules/
functions/package-lock.json
.firebase/
.claude/
.mcp.json
.opencode/
.vscode/
node_modules/
tests/node_modules/
tests/test-results/
tests/nul
tests/*.png
_archive_review/
temp/
screenshots/
test-results/
playwright-report/
public/pages/debug-review.html
docs/archive/session-logs/
functions/.env
functions/.env.local
functions/.env.*
*.log
*.backup*
*.old
*.broken
tmpclaude-*
tmp-*
reports/
*.sh
*.py
# …except the darts-vision Python subproject's source (root-level only — the
# venv stays ignored via darts-vision/.gitignore, so this can't pull in its .py)
!darts-vision/*.py
service-account.json
functions/service-account-key.json
functions/tmpclaude-*-cwd
nul
.codex/
tmp/

# ONNX spike test models (throwaway)
public/models/spike/
public/models/dart/

# QA scratch artifacts — every underscore-prefixed dev harness, snapshot,
# render, cert, log. Canonical tools (serve-public.mjs, droid-eval*.mjs) have
# no underscore and stay tracked.
scripts/qa/_*

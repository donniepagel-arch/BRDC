# BRDC dart-detection dataset (auto-labeled by the classical engine, human-corrected)
path: E:/projects/brdc-firebase/darts-vision/dataset
train: images/train
val: images/val
names:
  0: cal_top      # double-ring outer edge, top of 20
  1: cal_right    # double-ring outer edge, right of 6
  2: cal_bottom   # double-ring outer edge, bottom of 3
  3: cal_left     # double-ring outer edge, left of 11
  4: dart         # dart tip

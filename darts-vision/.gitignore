# heavy / regenerable — not source
.venv/
runs/
__pycache__/
*.pt
*.onnx
# dataset is captured data, not source (keep the config)
dataset/images/
dataset/labels/
# scratch dev artifacts — any underscore-prefixed output (cvcal renders, snaps,
# label-checks, install logs), in any subdir (incl. synth/_preview, synth/backgrounds/_*)
_*
flywheel.log
flywheel_state.json

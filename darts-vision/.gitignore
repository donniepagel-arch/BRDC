# heavy / regenerable — not source
.venv/
runs/
__pycache__/
*.pt
*.onnx
# dataset is captured data, not source (keep the config)
dataset/images/
dataset/labels/
# scratch install/log artifacts
_*.ps1
_*.log
_*.txt
flywheel.log
flywheel_state.json

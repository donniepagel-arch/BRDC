{
  "name": "brdc-tests",
  "version": "1.0.0",
  "description": "Automated browser tests for BRDC",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:debug": "npx playwright test --headed --slow-mo=500"
  },
  "type": "commonjs",
  "dependencies": {
    "@playwright/test": "^1.58.1"
  }
}

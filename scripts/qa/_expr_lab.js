(async()=>{const tab=[...document.querySelectorAll('.mode-tab')].find(b=>b.dataset.mode==='selftest');tab.click();document.getElementById('btnRunTests').click();await new Promise(r=>setTimeout(r,600));const result=document.getElementById('testList').innerText;[...document.querySelectorAll('.mode-tab')].find(b=>b.dataset.mode==='camera').click();const src=[...document.querySelectorAll('script')].map(s=>s.src||'').find(s=>s.includes('autoscore-engine'))||document.documentElement.innerHTML.match(/autoscore-engine[^'"]*/)?.[0];return {result, engineRef: src};})()
